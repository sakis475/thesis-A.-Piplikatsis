import { SimilarityPipe } from './similarity.pipe';

describe('SimilarityPipe', () => {
  it('create an instance', () => {
    const pipe = new SimilarityPipe();
    expect(pipe).toBeTruthy();
  });
});
